# Variables de entorno del Frontend — NN Auth System
# Copia este archivo como .env y ajusta los valores según tu entorno.
# NUNCA versionar el archivo .env real — contiene URLs y configuración sensible.

# URL base del backend Spring Boot.
# En desarrollo apunta al servidor local (puerto 8080).
# En producción, reemplazar por la URL real del servidor.
VITE_API_BASE_URL=http://localhost:8080
